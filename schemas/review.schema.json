{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/chadcoulter/REDE-Disclosure/schemas/review.schema.json",
  "title": "Review",
  "description": "An evaluation of an Implementation that assesses whether it meets its stated goals.",
  "type": "object",
  "required": ["type", "id", "title", "subject", "created", "status"],
  "additionalProperties": false,
  "properties": {
    "type": {
      "type": "string",
      "const": "review",
      "description": "Must be 'review'."
    },
    "id": {
      "type": "string",
      "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$",
      "description": "Globally unique slug identifier using lowercase kebab-case."
    },
    "title": {
      "type": "string",
      "minLength": 1,
      "description": "A descriptive title for this review."
    },
    "subject": {
      "type": "string",
      "description": "ID of the Implementation being reviewed."
    },
    "created": {
      "type": "string",
      "format": "date",
      "description": "ISO 8601 date when this review was requested."
    },
    "created_by": {
      "type": "string",
      "description": "GitHub username of the reviewer."
    },
    "status": {
      "type": "string",
      "enum": ["pending", "in-progress", "approved", "needs-revision", "rejected"],
      "description": "Lifecycle status of the review."
    },
    "completed": {
      "type": "string",
      "format": "date",
      "description": "ISO 8601 date when this review was completed (if applicable)."
    },
    "criteria": {
      "type": "array",
      "items": { "type": "string" },
      "default": [],
      "description": "Specific criteria or dimensions evaluated in this review."
    },
    "tags": {
      "type": "array",
      "items": { "type": "string" },
      "default": [],
      "description": "Free-form labels for discovery and filtering."
    }
  }
}
